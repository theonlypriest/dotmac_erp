# ============================================================================
# GENERATED by dotmac-deployment-foundation. Do not edit; edit
# deploy/product.toml and re-run `dotmac-deploy render`.
#
# product:          dotmac-erp
# schema:           ProductDeploymentSpec.v1
# image:            ghcr.io/michaelayoade/dotmac_erp@sha256:0d29ae34f99fbb9a97c0225666feca2428b8d1aa5b66a4d1a2dc642e486586b3
# descriptor image: ghcr.io/michaelayoade/dotmac_erp@sha256:0d29ae34f99fbb9a97c0225666feca2428b8d1aa5b66a4d1a2dc642e486586b3
# source revision:  6140ed40949449e5d6d42e5737aaded909f25fd3
# manifest digest:  sha256:9c3547745e453ffbd9339ce0d662af64a5071067087f16008f4630aae8b469b9
# ============================================================================

services:

  redis:
    image: "redis:7"
    command:
      - redis-server
      - --requirepass
      - "${REDIS_PASSWORD}"
    environment:
      REDIS_PASSWORD: "${REDIS_PASSWORD:?REDIS_PASSWORD must be set}"
    ports:
      - "127.0.0.1:6382:6379"
    healthcheck:
      test:
        - CMD
        - redis-cli
        - -a
        - "${REDIS_PASSWORD}"
        - ping
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s
    deploy:
      replicas: 1
      resources:
        limits:
          cpus: "0.5"
          memory: 512m
          pids: 256
    pids_limit: 256
    networks:
      - dotmac-erp_net
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: "5"
    restart: unless-stopped

  migrate:
    image: "ghcr.io/michaelayoade/dotmac_erp@sha256:0d29ae34f99fbb9a97c0225666feca2428b8d1aa5b66a4d1a2dc642e486586b3"
    command:
      - alembic
      - upgrade
      - heads
    depends_on:
      redis:
        condition: service_healthy
    environment:
      MIGRATION_DATABASE_URL: "${MIGRATION_DATABASE_URL:?MIGRATION_DATABASE_URL must be set}"
    networks:
      - dotmac-erp_net
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 512m
          pids: 256
    pids_limit: 256
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: "5"
    read_only: true
    security_opt:
      - "no-new-privileges:true"
    cap_drop:
      - ALL
    user: "10001:10001"
    tmpfs:
      - /tmp
    stop_grace_period: 300s
    restart: "no"

  app:
    image: "ghcr.io/michaelayoade/dotmac_erp@sha256:0d29ae34f99fbb9a97c0225666feca2428b8d1aa5b66a4d1a2dc642e486586b3"
    command:
      - gunicorn
      - -c
      - gunicorn.conf.py
      - "app.main:app"
    environment:
      CELERY_BROKER_URL: "${CELERY_BROKER_URL:?CELERY_BROKER_URL must be set}"
      CELERY_RESULT_BACKEND: "${CELERY_RESULT_BACKEND:?CELERY_RESULT_BACKEND must be set}"
      DATABASE_URL: "${DATABASE_URL:?DATABASE_URL must be set}"
      METRICS_TOKEN: "${METRICS_TOKEN:?METRICS_TOKEN must be set}"
      OPENBAO_ADDR: "${OPENBAO_ADDR:?OPENBAO_ADDR must be set}"
      OPENBAO_TOKEN: "${OPENBAO_TOKEN:?OPENBAO_TOKEN must be set}"
      REDIS_PASSWORD: "${REDIS_PASSWORD:?REDIS_PASSWORD must be set}"
      REDIS_URL: "${REDIS_URL:?REDIS_URL must be set}"
      S3_ACCESS_KEY: "${S3_ACCESS_KEY:?S3_ACCESS_KEY must be set}"
      S3_ENDPOINT_URL: "${S3_ENDPOINT_URL:?S3_ENDPOINT_URL must be set}"
      S3_SECRET_KEY: "${S3_SECRET_KEY:?S3_SECRET_KEY must be set}"
      BANKING_AUTO_MATCH_ENABLED: "false"
      PYTHONUNBUFFERED: "1"
      S3_BUCKET_NAME: dotmac-erp
    depends_on:
      migrate:
        condition: service_completed_successfully
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - "import sys,urllib.request;sys.exit(0 if urllib.request.urlopen('http://127.0.0.1:8002/health/ready',timeout=3).status==200 else 1)"
      interval: 5s
      timeout: 3s
      retries: 6
      start_period: 30s
    ports:
      - "127.0.0.1:8002:8002"
    networks:
      - dotmac-erp_net
    deploy:
      replicas: 1
      resources:
        limits:
          cpus: "2.0"
          memory: 2g
          pids: 512
    pids_limit: 512
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: "5"
    read_only: true
    security_opt:
      - "no-new-privileges:true"
    cap_drop:
      - ALL
    user: "10001:10001"
    tmpfs:
      - /tmp
    stop_grace_period: 35s
    restart: unless-stopped
    labels:
      io.dotmac.health.live.path: /health/live

  beat:
    image: "ghcr.io/michaelayoade/dotmac_erp@sha256:0d29ae34f99fbb9a97c0225666feca2428b8d1aa5b66a4d1a2dc642e486586b3"
    command:
      - celery
      - -A
      - app.celery_app
      - beat
      - -l
      - info
    environment:
      CELERY_BROKER_URL: "${CELERY_BROKER_URL:?CELERY_BROKER_URL must be set}"
      CELERY_RESULT_BACKEND: "${CELERY_RESULT_BACKEND:?CELERY_RESULT_BACKEND must be set}"
      DATABASE_URL: "${DATABASE_URL:?DATABASE_URL must be set}"
      OPENBAO_ADDR: "${OPENBAO_ADDR:?OPENBAO_ADDR must be set}"
      OPENBAO_TOKEN: "${OPENBAO_TOKEN:?OPENBAO_TOKEN must be set}"
      REDIS_PASSWORD: "${REDIS_PASSWORD:?REDIS_PASSWORD must be set}"
      REDIS_URL: "${REDIS_URL:?REDIS_URL must be set}"
      S3_ACCESS_KEY: "${S3_ACCESS_KEY:?S3_ACCESS_KEY must be set}"
      S3_ENDPOINT_URL: "${S3_ENDPOINT_URL:?S3_ENDPOINT_URL must be set}"
      S3_SECRET_KEY: "${S3_SECRET_KEY:?S3_SECRET_KEY must be set}"
      BANKING_AUTO_MATCH_ENABLED: "false"
      PYTHONUNBUFFERED: "1"
      S3_BUCKET_NAME: dotmac-erp
    depends_on:
      migrate:
        condition: service_completed_successfully
      redis:
        condition: service_healthy
    networks:
      - dotmac-erp_net
    deploy:
      replicas: 1
      resources:
        limits:
          cpus: "0.5"
          memory: 512m
          pids: 256
    pids_limit: 256
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: "5"
    read_only: true
    security_opt:
      - "no-new-privileges:true"
    cap_drop:
      - ALL
    user: "10001:10001"
    tmpfs:
      - /tmp
    stop_grace_period: 30s
    restart: unless-stopped

  worker:
    image: "ghcr.io/michaelayoade/dotmac_erp@sha256:0d29ae34f99fbb9a97c0225666feca2428b8d1aa5b66a4d1a2dc642e486586b3"
    command:
      - celery
      - -A
      - app.celery_app
      - worker
      - -l
      - info
    environment:
      CELERY_BROKER_URL: "${CELERY_BROKER_URL:?CELERY_BROKER_URL must be set}"
      CELERY_RESULT_BACKEND: "${CELERY_RESULT_BACKEND:?CELERY_RESULT_BACKEND must be set}"
      DATABASE_URL: "${DATABASE_URL:?DATABASE_URL must be set}"
      OPENBAO_ADDR: "${OPENBAO_ADDR:?OPENBAO_ADDR must be set}"
      OPENBAO_TOKEN: "${OPENBAO_TOKEN:?OPENBAO_TOKEN must be set}"
      REDIS_PASSWORD: "${REDIS_PASSWORD:?REDIS_PASSWORD must be set}"
      REDIS_URL: "${REDIS_URL:?REDIS_URL must be set}"
      S3_ACCESS_KEY: "${S3_ACCESS_KEY:?S3_ACCESS_KEY must be set}"
      S3_ENDPOINT_URL: "${S3_ENDPOINT_URL:?S3_ENDPOINT_URL must be set}"
      S3_SECRET_KEY: "${S3_SECRET_KEY:?S3_SECRET_KEY must be set}"
      BANKING_AUTO_MATCH_ENABLED: "false"
      PYTHONUNBUFFERED: "1"
      S3_BUCKET_NAME: dotmac-erp
    depends_on:
      migrate:
        condition: service_completed_successfully
      redis:
        condition: service_healthy
    networks:
      - dotmac-erp_net
    deploy:
      replicas: 1
      resources:
        limits:
          cpus: "1.0"
          memory: 1g
          pids: 512
    pids_limit: 512
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: "5"
    read_only: true
    security_opt:
      - "no-new-privileges:true"
    cap_drop:
      - ALL
    user: "10001:10001"
    tmpfs:
      - /tmp
    stop_grace_period: 120s
    restart: unless-stopped

networks:
  dotmac-erp_net: {}
